export enum SidebarTypes {
  top = 'top', // 顶部-横向通栏
  topRight = 'top-right', // 顶部居右
  left = 'left', // 左侧-纵向通栏
  leftBottom = 'left-bottom', // 左侧居下
  hidden = 'hidden' // 隐藏
}
export enum NavbarTypes {
  top = 'top',
  topRight = 'top-right',
  leftBottom = 'left-bottom',
  hidden = 'hidden'
}
export enum BreadcrumbTypes {
  inNavbar = 'in',
  outNavbar = 'out',
  hidden = 'hidden'
}
export enum TabTypes {
  topRight = 'top-right',
  hidden = 'hidden'
}

